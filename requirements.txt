# Dependencias diretas do projeto (as transitivas o pip resolve sozinho).

# Reconhecimento de voz (STT) - usa a Google Web Speech API (sem chave).
SpeechRecognition==3.17.0
PyAudio==0.2.14

# Sintese de voz (TTS) - Microsoft Edge TTS, voz neural pt-BR.
edge-tts==7.2.8
pygame==2.6.1

# IA generativa - Google Gemini (conversa + classificacao de intencao).
google-generativeai==0.8.3

# Reconhecimento facial offline - Haar Cascade + LBPH.
opencv-contrib-python==4.10.0.84
numpy==1.26.4

# Screenshot.
pillow==10.4.0

# APIs de clima / cambio.
requests==2.32.3

# Reconhecimento de intencao (fuzzy matching tolerante a variacoes de fala).
rapidfuzz==3.10.1

# Terminal colorido no Windows.
colorama==0.4.6

# Leitura do .env (chave do Gemini).
python-dotenv==1.0.1

# Testes.
pytest==8.3.3

# OPCIONAL: SSL/TLS certificate verification para redes corporativas (requer Python >=3.10).
# Para ambientes com proxy corporativo, Zscaler ou antivirus com inspeção de TLS,
# faça upgrade para Python 3.10+ e instale: pip install truststore
# truststore==0.10.4
